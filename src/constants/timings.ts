// Delay before revealing the correct/wrong answer after selection
export const ANSWER_REVEAL_DELAY = 3000;

// Delay before advancing to the next question after revealing the answer
export const NEXT_QUESTION_DELAY = 1000;
